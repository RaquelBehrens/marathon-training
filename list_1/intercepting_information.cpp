#include <stdio.h>

int main() {
    int a, b, c, d, e, f, g, h;
    scanf("%d %d %d %d %d %d %d %d", &a, &b, &c, &d, &e, &f, &g, &h);

    if (a == 9) {
        printf("F\n");
        return 0;
    }
    if (b == 9) {
        printf("F\n");
        return 0;
    }
    if (c == 9) {
        printf("F\n");
        return 0;
    }
    if (d == 9) {
        printf("F\n");
        return 0;
    }
    if (e == 9) {
        printf("F\n");
        return 0;
    }
    if (f == 9) {
        printf("F\n");
        return 0;
    }
    if (g == 9) {
        printf("F\n");
        return 0;
    }
    if (h == 9) {
        printf("F\n");
        return 0;
    }

    printf("S\n");    
    return 0;
}
