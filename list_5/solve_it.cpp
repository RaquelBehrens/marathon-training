#include <bits/stdc++.h>

using namespace std;

int p, q, r, s, t, u;

double f(double x) {
	return (p * exp(-x) + q * sin(x) + r * cos(x) + s * tan(x) + t * x * x + u);
}

int main()
{
    int n, m;
    int property[500][500] = {0};

    while (cin >> p >> q >> r >> s >> t >> u) {
      
    }

    return 0;
}
